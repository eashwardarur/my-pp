# Server config
PORT=8080
# Comma-separated list of allowed origins for CORS, or omit to allow all
# CORS_ORIGIN=http://localhost:5173

# Firebase Admin configuration (choose ONE of the options below)
# Option 1: Base64-encoded service account JSON (recommended for environments where files are hard to mount)
# FIREBASE_SERVICE_ACCOUNT_JSON=eyJ0eXAiOiJKV1QiLCJhbGciOi...

# Option 2: Application Default Credentials (set path to JSON file)
# GOOGLE_APPLICATION_CREDENTIALS=F:\\path\\to\\service-account.json

# Optionally set for minimal initialization (useful with emulators)
# FIREBASE_PROJECT_ID=ace-76929
